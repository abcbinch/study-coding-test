c#은 텍스트 출력용과 윈도우 폼 출력용이 다르다?

c# 사용법을 검색해서 따라하는 중이다.
일단 하나를 정해서 그것만 따라한다.
끝까지 해 보고 안 되면 그 때 다른 방법을 시도한다.

이 사람이 알려준 c# 확장팩이 안 보인다.

c#프로젝트를 만들 폴더로 이동한다.
그 다음 dotnet new console을 실행한다.
dotnet new console --use-program-main
이렇게 입력하면 구 버전 템플릿이 생성된다고 한다.
구 버전은 자바 프로젝트처럼 클래스와 실행 클래스가 전부 입력되어 있는 파일이다.

.csproj 파일에는 파일의 태그 정보가 담겨있다. 프로젝트 파일이다.
.cs 파일에 코드를 입력하면 된다.

실행할 때는 
터미널에 dotnet run 이라고 치면 된다.

using System에서 
using은 일종의 import 같은 건가 보다.